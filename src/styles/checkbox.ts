import { StyleSheet } from 'react-native';

export const checkbox = StyleSheet.create({
  text: {
    fontSize: 14,
    fontWeight: '400',
    lineHeight: 16,
    height: 16,
    marginLeft: 8,
  },
  icon: {
    borderWidth: 0,
    borderRadius: 0,
  },
});
